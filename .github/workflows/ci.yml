name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # --------------------------
      # 4️⃣ Run Jest Unit Tests with HTML + Coverage
      # --------------------------
      - name: Run Jest Unit Tests
        run: |
          mkdir -p reports/jest
          npx jest "Unit Test/**/*.test.js" --ci --verbose --coverage \
            --coverageReporters=text --reporters=default \
            --reporters=jest-html-reporters \
            --outputPath=reports/jest/jest-report.html
        continue-on-error: false

      - name: Upload Jest HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: jest-html-report
          path: reports/jest/jest-report.html

      - name: Upload Jest Coverage JSON
        uses: actions/upload-artifact@v4
        with:
          name: jest-coverage
          path: coverage/coverage-final.json

      # --------------------------
      # 5️⃣ Install live-server
      # --------------------------
      - name: Install Live Server
        run: npm install -g live-server

      # --------------------------
      # 6️⃣ Start Live Server for Cypress
      # --------------------------
      - name: Start Live Server
        run: |
          live-server ./ --port=5500 &
          npx wait-on http://127.0.0.1:5500
        shell: bash

      # --------------------------
      # 7️⃣ Run Cypress E2E Tests with Mochawesome
      # --------------------------
      - name: Run Cypress E2E Tests
        run: |
          mkdir -p reports/cypress
          npx cypress run \
            --browser chrome \
            --headless \
            --reporter mochawesome \
            --reporter-options reportDir=reports/cypress,overwrite=false,html=true,json=true,charts=true,screenshotsFolder=reports/cypress/screenshots,videosFolder=reports/cypress/videos

      # Upload Cypress HTML/JSON Reports
      - name: Upload Cypress Mochawesome Reports
        uses: actions/upload-artifact@v4
        with:
          name: cypress-reports
          path: reports/cypress

      # Upload Cypress Videos
      - name: Upload Cypress Videos
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: reports/cypress/videos

      # --------------------------
      # 8️⃣ Placeholder for Future Test Automation
      # --------------------------
      - name: Test Automation Placeholder
        run: |
          echo "Future automation tests (API, integration, performance) will save reports here."
          mkdir -p reports/future-tests
